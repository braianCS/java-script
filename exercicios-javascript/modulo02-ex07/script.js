// EXERCÍCIO 07 | MÓDULO 2
// Objetivo: reescrever decisões simples usando o operador ternário.
// Estrutura do ternário: condição ? valorSeVerdadeiro : valorSeFalso

const idade = 17;

// Tarefa 1: este código com if/else já funciona. NÃO apague, apenas leia:
let acessoComIf;
if (idade >= 18) {
  acessoComIf = "permitido";
} else {
  acessoComIf = "negado";
}
console.log(acessoComIf);

// Tarefa 2: agora crie uma const "acessoComTernario" que faça a MESMA coisa que o
//         if/else acima, mas usando o operador ternário em uma única linha.


// Tarefa 3: use console.log() para mostrar "acessoComTernario" e confira se o
//         resultado é igual ao de "acessoComIf".


// Tarefa 4: dado o código abaixo, reescreva-o usando if/else (o caminho contrário
//         do que você fez até aqui):
const estoque = 5;
const mensagemEstoque = estoque > 0 ? "Disponível" : "Esgotado";
// escreva seu if/else aqui embaixo, guardando o resultado em uma variável
// chamada "mensagemEstoqueComIf", e compare com "mensagemEstoque"

