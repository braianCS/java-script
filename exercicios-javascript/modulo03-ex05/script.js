// EXERCÍCIO 05 | MÓDULO 3
// Objetivo: reescrever um for como while. No while, você mesmo controla o
// início e a atualização da variável (o JavaScript não faz isso por você).

// Este for já funciona. NÃO apague, apenas leia com atenção:
for (let numero = 1; numero <= 5; numero++) {
  console.log(numero);
}

// Tarefa 1: declare uma variável com let chamada "contador", começando em 1
//         (isso equivale à parte de "inicialização" do for acima).


// Tarefa 2: escreva um while(contador <= 5) que faça a MESMA coisa que o for
//         acima: mostrar "contador" no console a cada volta.


// Tarefa 3: dentro do bloco do while, NÃO esqueça de incrementar "contador"
//         (contador++), ou o laço nunca vai terminar (loop infinito).


// Tarefa 4: compare a saída do while com a saída do for. Devem ser iguais.

